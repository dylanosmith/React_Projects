import React, {useState} from 'react';

interface TabProps {
    TabNames:string[];
    TabContent: Function;
}

const Tabs:React.FC<TabProps> = ({TabNames, TabContent}) => {
    const handleClick = () => {

    }
    return(

    )
}
export default Tabs;